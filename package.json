{
  "name": "terminal-shop",
  "version": "0.0.0",
  "sideEffects": false,
  "scripts": {
    "dev": "sst dev",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "typecheck": "tsc -noEmit",
    "sso": "aws sso login --sso-session=ironbay",
    "postinstall": "patch-package"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "patch-package": "^8.0.0",
    "sst": "3.0.11"
  },
  "devDependencies": {
    "@types/shippo": "^1.7.8"
  }
}
